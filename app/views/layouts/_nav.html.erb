<nav class="navbar navbar-expand-md navbar-light bg-light fixed-top">
  <div class="container">
    <a class="navbar-brand" href="<%= root_path %>"><span class="light">Hodler</span><span class="strong">Zone</span></a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarsExampleDefault" aria-controls="navbarsExampleDefault" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarsExampleDefault">
      <ul class="navbar-nav ml-auto">
        <% if user_signed_in? %>
          <li class="nav-item">
            <a class="nav-link" href="<%= notifications_path %>">
              <% if !current_user.get_unread_alerts.empty? %>
                Alerts<span class="badge badge-secondary"><%= current_user.get_unread_alerts.length %></span>
              <% else %>
                Alerts
              <% end %>
            </a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="<%= user_path(current_user) %>"><%= current_user.first_name %></a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="<%= destroy_user_session_path %>" data-method="delete">Sign Out</a>
          </li>
        <% else %>
          <li class="nav-item">
            <a class="nav-link" href="<%= new_user_session_path %>">Sign in</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="<%= new_user_registration_path %>">Sign up</a>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
